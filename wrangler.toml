# Cloudflare Images is the primary photo CDN (imagedelivery.net).
# Git `public/images` and `public/og-image.jpg` remain the backup.
# R2 is optional tertiary storage — do not orange-cloud www (Vercel serves the app).
# Custom domain (DNS-only / gray cloud): media.aliantehomesforsale.com → this bucket.
# Public object keys match git paths: og-image.jpg, images/*.jpg

name = "aliante-homes-media"
compatibility_date = "2026-09-14"
account_id = "2cc579c1ec9e426ed585e933ebf4753b"

[[r2_buckets]]
binding = "MEDIA"
bucket_name = "aliante-homes-media"
